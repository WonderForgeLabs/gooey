// SessionService — the streaming control plane.
//
// One bidi stream, mirroring the framework's own shape: the client
// side is ONE ordered stream of acts, exactly as the terminal is one
// ordered input.Event stream; the server side is one ordered stream of
// events, framed on the app's composed frames.
//
// The consistency rule (issue #49's torn-read lesson): everything that
// changed within one composed frame — property deltas, damage, the
// structure swap that caused them — arrives in ONE FrameDelta message.
// A client can never observe values without the frame they belong to,
// by construction; there is no delta channel to tear away from its
// markup.

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: gooey/control/v1/session.proto

package controlv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	SessionService_Attach_FullMethodName = "/gooey.control.v1.SessionService/Attach"
)

// SessionServiceClient is the client API for SessionService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// SessionService attaches a client to a running app for the lifetime
// of the stream. Push replaces polling (issue #49): a subscribed
// client hears about a change when the frame containing it composes,
// not on the next 400ms tick.
type SessionServiceClient interface {
	// Attach opens a session. The first client message must be
	// `subscribe`; every later one is an `act`. Server messages start
	// with `welcome` and then interleave act results with subscribed
	// events. Closing either direction ends the session; the app
	// outlives every session.
	Attach(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[AttachRequest, AttachResponse], error)
}

type sessionServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewSessionServiceClient(cc grpc.ClientConnInterface) SessionServiceClient {
	return &sessionServiceClient{cc}
}

func (c *sessionServiceClient) Attach(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[AttachRequest, AttachResponse], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &SessionService_ServiceDesc.Streams[0], SessionService_Attach_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[AttachRequest, AttachResponse]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type SessionService_AttachClient = grpc.BidiStreamingClient[AttachRequest, AttachResponse]

// SessionServiceServer is the server API for SessionService service.
// All implementations must embed UnimplementedSessionServiceServer
// for forward compatibility.
//
// SessionService attaches a client to a running app for the lifetime
// of the stream. Push replaces polling (issue #49): a subscribed
// client hears about a change when the frame containing it composes,
// not on the next 400ms tick.
type SessionServiceServer interface {
	// Attach opens a session. The first client message must be
	// `subscribe`; every later one is an `act`. Server messages start
	// with `welcome` and then interleave act results with subscribed
	// events. Closing either direction ends the session; the app
	// outlives every session.
	Attach(grpc.BidiStreamingServer[AttachRequest, AttachResponse]) error
	mustEmbedUnimplementedSessionServiceServer()
}

// UnimplementedSessionServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedSessionServiceServer struct{}

func (UnimplementedSessionServiceServer) Attach(grpc.BidiStreamingServer[AttachRequest, AttachResponse]) error {
	return status.Error(codes.Unimplemented, "method Attach not implemented")
}
func (UnimplementedSessionServiceServer) mustEmbedUnimplementedSessionServiceServer() {}
func (UnimplementedSessionServiceServer) testEmbeddedByValue()                        {}

// UnsafeSessionServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to SessionServiceServer will
// result in compilation errors.
type UnsafeSessionServiceServer interface {
	mustEmbedUnimplementedSessionServiceServer()
}

func RegisterSessionServiceServer(s grpc.ServiceRegistrar, srv SessionServiceServer) {
	// If the following call panics, it indicates UnimplementedSessionServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&SessionService_ServiceDesc, srv)
}

func _SessionService_Attach_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(SessionServiceServer).Attach(&grpc.GenericServerStream[AttachRequest, AttachResponse]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type SessionService_AttachServer = grpc.BidiStreamingServer[AttachRequest, AttachResponse]

// SessionService_ServiceDesc is the grpc.ServiceDesc for SessionService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var SessionService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "gooey.control.v1.SessionService",
	HandlerType: (*SessionServiceServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Attach",
			Handler:       _SessionService_Attach_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "gooey/control/v1/session.proto",
}
